# Services

Future backend services are intentionally deferred for this milestone.

Planned service boundaries include:

- `api` for the future API Gateway/Lambda application boundary.
- `pdf-worker` for future asynchronous invoice PDF generation.
- `email-worker` for future outbound email processing.

No service package, backend implementation, AWS integration, Lambda handler, queue consumer, or deployment configuration exists yet.
